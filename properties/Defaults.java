package properties;

import javafx.scene.paint.Color;

/**
 * This is a class that holds default values for a Whiteboard.
 */
public class Defaults {
    public static final Color PANE_COLOR = Color.rgb(180, 200, 220);
    public static final Color TEXT_COLOR = Color.rgb(30, 60, 90);
}
