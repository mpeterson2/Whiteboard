package properties;

import javafx.scene.paint.Color;

/**
 * This is a class that holds default values for a Whiteboard.
 */
public class Defaults {
    public static final Color DEFAULT_PANE_COLOR = Color.rgb(180, 200, 220);
}
